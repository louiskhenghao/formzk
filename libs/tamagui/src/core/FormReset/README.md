# FormResetButton

Tamagui `Button` wired to the form context's reset handler. Also available as `Formzk.Tamagui.Reset`.

---

## Example

```tsx
import { Formzk } from '@formzk/tamagui';

<Formzk.Tamagui.Reset text="Clear" />;
```
